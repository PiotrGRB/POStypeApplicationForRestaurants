<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.piotrg.postypeapplicationforrestaurants">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <service android:name=".Network.NetworkConnectionService" />

        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.RoleManagerActivity" />
        <activity android:name=".Activity.NetworkActivity" />
        <activity android:name=".Activity.ConfigureMenuActivity" />
        <activity android:name=".Activity.CreateOrderActivity" />
        <activity android:name=".Activity.CreateOrderEditActivity" />
        <activity android:name=".Activity.ActiveOrdersActivity" />
        <activity android:name=".Activity.ActiveOrdersEditActivity" />
        <activity android:name=".Activity.ArchivesActivity">
        </activity>
    </application>

</manifest>